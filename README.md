# clawpack-docker-examples

To download dems:

```bash
$ bash get_dems.sh
```

To build the container

```bash
$ bash build.sh
```

Once in the container, choose an example from the `/home/examples` dir, then run `make all`
